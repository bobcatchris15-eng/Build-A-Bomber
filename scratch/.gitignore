# Axis-probe outputs are regenerated by run_probe.ps1, not authored.
# Keep the .json inputs and the report checked in; ignore the .glb
# outputs and the test_case*.glb scratch artifacts.
axis_probe.glb
axis_probe.json
test_case*.glb
